// App-wide constants
export const MAX_GYM_CAPACITY = 300;
export const DEFAULT_CURRENCY = 'USD';
export const DATE_FORMAT = 'MM/dd/yyyy';

// Status constants
export const MEMBER_STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  EXPIRED: 'expired',
} as const;